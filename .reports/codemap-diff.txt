Codemap update — 2026-08-26

First run: no prior docs/CODEMAPS existed, so this is a full initial scan,
not a diff. Wrote:
  docs/CODEMAPS/architecture.md
  docs/CODEMAPS/backend.md
  docs/CODEMAPS/frontend.md
  docs/CODEMAPS/data.md
  docs/CODEMAPS/dependencies.md

Scope scanned: backend/ (server.py, models.py, auth.py, tenancy.py,
lifecycle.py, seed.py, tests/), frontend/src/ (pages/, components/, context/,
lib/, App.js), db/migrations/, render.yaml, netlify.toml,
netlify/edge-functions/, requirements.txt, package.json.

Notable findings worth a human's attention:
- backend/server.py:1373-1376 — a manual `GET /leads` handler is dead code,
  shadowed by the make_crud-registered route above it. Comment in the source
  already flags this as intentionally left in place; no fix needed, just
  documented in backend.md so nobody edits the wrong handler.
- db/migrations/*.sql describes a SQL schema for a backend that does not
  exist yet; the live datastore is MongoDB. Flagged in architecture.md and
  data.md so it isn't mistaken for the current schema.
- tenancy.ENTITY_COLLECTION declares a "customer" -> "customers" workflow
  entity with no corresponding model/route found in models.py/server.py.
  Flagged in data.md — worth confirming with the team whether this is planned
  or stale.
- backend/requirements.txt lists boto3, requests-oauthlib, and typer with no
  usage found via grep in backend/*.py — flagged in dependencies.md as likely
  template leftovers rather than load-bearing.

Staleness: not applicable (first run).
